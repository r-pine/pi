# Runtime state — never commit
sessions/
state/
npm/node_modules/
npm/package-lock.json

# Understory — cloned source + live memory bundle (remove understory-memory/ line to commit shared memory)
understory/
understory-memory/

# Local overrides
trust.json
auth.json
