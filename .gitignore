# Runtime state — never commit
sessions/
state/
npm/node_modules/
npm/package-lock.json

# Understory — cloned source + live memory bundle (paths relative to installed .pi/)
/understory/
/understory-memory/

# Local overrides
trust.json
auth.json
